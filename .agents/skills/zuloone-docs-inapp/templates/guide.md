---
key: <вид>.<что>
title: <Заголовок в шапке drawer, 1–4 слова>
---

<!--
КАРКАС тела ⓘ-гайда. Удали комментарии перед сохранением.

Front-matter — для ФАЙЛОВОЙ формы гайда (волна 2 плана
docs/superpowers/plans/2026-09-23-documentation-and-training.md). Сегодня
файлов нет: `title` вбивается в поле «Title» в drawer, а в «Body» уходит
ТОЛЬКО тело, без блока `---`.

Объём: 3–8 абзацев. Больше — значит это страница вики, и её место в wiki/,
а здесь остаётся одна ссылка в конце.
-->

<!--
ПЕРВЫЙ АБЗАЦ — одно предложение: что это такое. Без «в этом разделе», без
«данная форма предназначена». Читатель проверяет, туда ли попал, за две
секунды.
-->
<Что это> — <чем является одним предложением>: <зачем существует>.

<!--
ДАЛЬШЕ — что выбрать и чем значения отличаются. Не перечисление подписей
(они у читателя перед глазами), а ПОСЛЕДСТВИЯ выбора: что изменится в
схеме, в остатках, в проведении, в цифре.

Вариантов два-четыре — маркированный список. Больше или у каждого есть
«что меняется» — таблица.
-->
- **<Значение или режим>** — <что происходит>. <Что меняется в поведении.>
- **<Значение или режим>** — <что происходит>. <Чем отличается от первого.>

| <Что выбираем> | Что меняется |
| --- | --- |
| `<значение>` | <какая колонка, какой контроль, какая цифра> |
| *(пусто)* | <умолчание и его последствие> |

<!--
КОД — только если без него не объяснить. Одна короткая иллюстрация, не урок.
-->
```csharp
movements.Add(new RegisterMovementSpec("<Регистр>")
    .Dim("<Измерение>", document.Ref("<Поле>"))
    .Res("<Ресурс>", line.Dec("<Поле>")));
```

<!--
ВРЕЗКИ — ровно две формы, ставь по надобности, не обе подряд.
-->
> 💡 **Лайфхак** <Короткий приём, экономящий шаги.>

> ⚠️ **Ловушка** <Что ломается и почему это не видно сразу.>

<!--
КАРТИНКА — alt становится ПОДПИСЬЮ под ней, пиши его как подпись.
Допустимые src: http(s)://, корневой /help/<имя>.png, вставленный
data:image/(png|jpeg|gif|webp). Снимок делается по zuloone-docs-screens.

Каталога frontend/public/help/ ещё НЕТ (волна 1 того же плана): сегодня
рабочий путь — вставить снимок из буфера, он приедет как data:image/…
Корневой /help/… заработает, когда каталог появится.
-->
![<Вкладка «X»: что именно на снимке видно>](/help/<тема>-<что>.png)

<!--
ХВОСТ — только если тема глубже экрана. Одна ссылка, с указанием, зачем идти.
-->
Подробнее: <вики разработчика → Тема> / [<название страницы>](…).
